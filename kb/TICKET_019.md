# TICKET-019

## Summary
Implemented `src/math.lua`, a small pure-Lua math helper module exposing:

- `add(a, b)`
- `sub(a, b)`
- `mul(a, b)`
- `div(a, b)`

## Validation
Attempted a Lua smoke test for all four functions, but the `lua` binary is not installed in this environment.
